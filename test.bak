(load "interpreter.scm")
(load "classParser.scm")
(require rackunit)

; Part Four Tests ******************************************************

(check-eq? (interpret "tests/4/1.txt") 15)
(check-eq? (interpret "tests/4/2.txt") 12)
(check-eq? (interpret "tests/4/3.txt") 125)
(check-eq? (interpret "tests/4/4.txt") 36)
(check-eq? (interpret "tests/4/5.txt") 54)
(check-eq? (interpret "tests/4/6.txt") 110)
(check-eq? (interpret "tests/4/7.txt") 26)
(check-eq? (interpret "tests/4/8.txt") 117)
(check-eq? (interpret "tests/4/9.txt") 32)
(check-eq? (interpret "tests/4/10.txt") 15)
(check-eq? (interpret "tests/4/11.txt") 123456)
(check-eq? (interpret "tests/4/12.txt") 5285)
(check-eq? (interpret "tests/4/13.txt") -716)

(display "All tests completed!")